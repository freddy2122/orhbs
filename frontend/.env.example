# Développement : laisser vide — le proxy Vite redirige /api vers Django (localhost:8000)
# VITE_API_URL=

# Production (Vercel) : URL du backend Render, sans slash final
# VITE_API_URL=https://orhsb-api.onrender.com
