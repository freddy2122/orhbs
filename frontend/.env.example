# Développement : laisser vide — le proxy Vite redirige /api vers Django (localhost:8000)
# VITE_API_URL=

# Production Vercel : ne pas définir VITE_API_URL.
# Les appels /api sont réécrits vers https://orhsb-api.onrender.com (vercel.json).
